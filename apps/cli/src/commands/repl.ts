/** @mirror DSH: apps/cli/src/commands/repl.ts */

export function cmdRepl(args: string[]): Promise<number> {
  void args;
  console.log('[aih repl] TODO: see lessons/03');
  return Promise.resolve(0);
}